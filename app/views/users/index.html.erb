<% provide(:title, 'Users') %>
<div id="user-overview" class="hero-unit">
  <%= heading 'User', 'Overview' %>

	<%= will_paginate %>

	<ul class="users-list">
		<%= render @users %>
	</ul>

	<%= will_paginate %>
</div>