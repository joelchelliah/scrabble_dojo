<div class="hero-unit">
	<h2><%= header_text @length, @first_letter %></h2>
	<hr/>
	<%= link_to 'Back', "/" %>

	<% alfabet().each do |letter| %>
	<%= words_starting_with(letter) %>
	<% end %>

	<%= link_to 'Back', "/" %>
</div>