<p><strong><%= @memo.name %></strong></p>
<div class="pull-left">
	<% if @memo.practice_disabled %>
		<p>Word count: </p>
	<% else %>
		<p>Health: </p>
		<p>Word count: </p>
		<p>Practice count: &nbsp; </p>
		<p>Best time: </p>
		<p>Avg. word time: </p>
	<% end %>
</div>
<% if @memo.practice_disabled %>
	<p><strong><%= @memo.word_list.split.count %></strong></p>
<% else %>
	<p><strong><%= show_health %></strong></p>
	<p><strong><%= @memo.word_list.split.count %></strong></p>
	<p><strong><%= @memo.num_practices %></strong></p>
	<p><strong><%= if @memo.best_time then "#{@memo.best_time} s" else "n/a" end %></strong></p>
	<% if @memo.best_time and !@memo.word_list.blank? %>
		<p><strong><%= "#{"%.1f" % (@memo.best_time / @memo.word_list.split(/\r?\n/).size)}" %> s</strong></p>
	<% else %>
		<p><strong> n/a </strong></p>
	<% end %>
<% end %>
<hr/>
<div class="center">
	<span class="icon-book"></span>
</div>
<% if @memo.practice_disabled %>
	<p> <strong>Practice</strong> mode has been disabled for this Memo. </p>
<% else %>
	<p> Once you are done revising, choose <strong>Practice</strong> mode to:</p>
	<ul>
		<li> Test your skills</li>
		<li> Replenish your health</li>
		<li> Set a new time record</li>
	</ul>
<% end %>