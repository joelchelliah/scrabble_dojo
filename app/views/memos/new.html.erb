<% provide(:title, 'New memo') %>
<div id="memo-form" class="hero-unit">
	<h1>Memo</h1>
	<h2>Create</h2>
	<hr/>
	<%= form_for(@memo) do |f| %>
	  <fieldset>
			<%= render 'fields', f: f %>
			<div class="row">
	      <div class="span1"><%= f.submit "Create", id: "memo-form-submit", class: "btn btn-primary" %></div>
	      <div class="span1"><%= link_to 'Back', memos_path, class: "btn" %></div>
	    </div>
	  </fieldset>
	<% end %>
</div>
