<% provide(:title, 'Sign up') %>
<div id="sign-up" class="hero-unit">
	<h1>Account</h1>
	<h2>Sign up</h2>
	<hr/>
	<strong> Currently unavailable</strong>

	<!--<%= form_for(@user) do |f| %>
		<fieldset>
	  	<%= render 'fields', f: f, password_placeholder: "Password" %>
	  	<div class="row">
		    <div class="span3 offset3"><%= f.submit "Create my account", class: "btn btn-primary" %></div>
		    <div class="span1"><%= link_to "Cancel", root_url, class: "btn" %></div>
		  </div>
		</fieldset>
	<% end %>-->
</div>
